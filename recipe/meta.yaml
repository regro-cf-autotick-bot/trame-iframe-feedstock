{% set name = "trame-iframe" %}
{% set version = "1.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/trame-iframe-{{ version }}.tar.gz
  sha256: 85c93a5f23e5094bf3d8b6d248bc3b2da222c65d766e059401df672cd7ae34cd

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - trame-client

test:
  imports:
    - trame
    - trame_iframe
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Kitware/trame-iframe
  summary: Helper for iframe/cross-origin communication for trame
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jourdain
    - psavery
